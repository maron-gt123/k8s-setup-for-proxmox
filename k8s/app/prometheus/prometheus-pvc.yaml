apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: prometheus
  name: prometheus-pvc
  labels:
    app: prometheus
spec:
  storageClassName: "prometheus"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
