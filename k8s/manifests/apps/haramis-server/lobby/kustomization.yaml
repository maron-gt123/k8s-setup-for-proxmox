apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: haramis-lobby-sv
resources:
  # namespace
  - ./ns-haramis-lobby-sv.yaml
  # template
  - .../templates/minecraft-server

patches:
