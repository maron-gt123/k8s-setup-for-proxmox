apiVersion: batch/v1
kind: CronJob
metadata:
  name: minecraft-frontend-restart-warning
spec:
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: notifier
              env:
                - name: RCON_HOST
                  value: mc-s1-mc-service.mc-haramis.svc.cluster.local:25565
