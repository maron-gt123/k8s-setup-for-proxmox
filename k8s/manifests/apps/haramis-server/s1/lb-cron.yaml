apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
  namespace: default
  labels:
    app: nginx
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - name: http
      protocol: TCP
      port: 80        # 外部公開ポート
      targetPort: 80  # Pod内のコンテナポート
