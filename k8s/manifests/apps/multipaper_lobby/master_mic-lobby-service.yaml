apiVersion: v1
kind: Service
metadata:
  namespace: mic-lobby-sv
  name: master_mic-lobby-service
  labels:
    app: primary-sv
spec:
  type: ClusterIP
  ports:
    - name: multipaper-external-port
      port: 35353
      protocol: TCP
      targetPort: 35353
  selector:
    app: primary-sv
