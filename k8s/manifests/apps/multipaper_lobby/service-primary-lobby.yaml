apiVersion: v1
kind: Service
metadata:
  namespace: mic-lobby-sv
  name: primary-sv-service
  labels:
    app: primary-sv
spec:
  type: ClusterIP
  ports:
    - name: multipaper-int
      port: 35353
      protocol: TCP
      targetPort: 35353
    - name: multipaper-ex
      port: 25565
      protocol: TCP
      targetPort: 25565
  selector:
    app: primary-sv
