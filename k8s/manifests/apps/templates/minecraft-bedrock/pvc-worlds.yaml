apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bedrock-world-pvc
  labels:
    app: bedrock-world-pvc
spec:
  storageClassName: mc-bedrock-sc
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
