apiVersion: v1
kind: Service
metadata:
  name: tiktok-stream
  labels:
    app: minecraft
spec:
  type: ClusterIP
  ports:
  - name: tiktok-stream
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: minecraft
