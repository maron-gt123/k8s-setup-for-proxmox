apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf
data:
  web.conf: |
    server {
        listen 80;
        server_name localhost;

        root /usr/share/nginx/debug;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
